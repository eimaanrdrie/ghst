import Link from "next/link";
import { Bot, ExternalLink, LockKeyhole, ShieldCheck } from "lucide-react";

export default function AISandboxPage() {
  return <main className="sandbox"><section className="card card-pad sandbox-card"><div className="trust-chip" style={{ width: "fit-content" }}><ShieldCheck size={14} /> Organisation-approved destination</div><h1>GHST Sandbox AI</h1><p style={{ color: "var(--muted)", lineHeight: 1.6 }}>This page is the explicit mock approved downstream used for the hackathon. The Chrome extension injects its Protected Prompt Composer here; the native composer remains disabled until a valid one-time clearance reaches the gateway.</p><div className="notice"><LockKeyhole size={17} />No prompt typed into GHST is transmitted here before the Policy Decision Point returns a final ALLOW and the gateway validates the exact prompt digest.</div><div className="card card-pad native-composer" style={{ marginTop: 20 }}><div className="field"><label>Native destination composer · locked by governance</label><textarea id="ghst-native-ai-composer" placeholder="The extension injects only released content here…" /></div><button className="button button-secondary" disabled><Bot size={16} />Native send disabled</button></div><div className="button-row" style={{ marginTop: 18 }}><Link className="button button-primary" href="/employee"><ShieldCheck size={16} />Open protected composer</Link><a className="button button-secondary" href="http://localhost:8000/docs" target="_blank"><ExternalLink size={15} />API contract</a></div></section></main>;
}

